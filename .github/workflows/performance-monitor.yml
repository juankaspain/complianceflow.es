name: Performance Monitor

# ✅ DESACTIVADO - Solo manual
on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 0 * * 0'  # Desactivado

jobs:
  monitor:
    name: Performance Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - run: echo "Performance monitoring disabled to save CI minutes"
      - run: echo "Run manually when needed via workflow_dispatch"
