name: Backup

# ✅ Solo una vez al día
on:
  schedule:
    - cron: '0 2 * * *'  # 2am diario
  workflow_dispatch:

jobs:
  backup:
    name: Repository Backup
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - run: echo "Backup job - consider using external service"
      - run: echo "GitHub keeps history automatically"
